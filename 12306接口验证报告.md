# 12306 接口连通性验证报告

> 验证时间：2026-09-18
> 验证对象：`kyfw.12306.cn` 公开查询接口
> 验证方式：Python 标准库（`urllib`）直连，零第三方依赖
> 结论：**接口可用，方案可行。但限流是最大的工程约束。**

---

## 一、核心结论速览

| 项目 | 状态 | 说明 |
|---|---|---|
| 站点字典接口 | ✅ 完全可用 | 3384 个车站，稳定，无限制 |
| 经停站接口 | ✅ 完全可用 | 数据完整，含到达/发车/停车时长 |
| 余票查询接口 | ⚠️ 可用但强限流 | 高频请求后会被拦截，需严格节流 |
| 数据加密层 | ⚠️ 有变化 | `result` 字段新增编码层，需处理 |
| 整体可行性 | ✅ 可行 | 无登录要求，数据结构稳定 |

---

## 二、接口实测数据

### 2.1 站点字典

```
GET https://kyfw.12306.cn/otn/resources/js/framework/station_name.js
```

- HTTP 200，响应 136,152 字节
- 解析出 **3384 个车站**
- 格式：`@拼音|中文名|三字码|拼音简写|序号`
- **建议本地缓存**，此文件极少变动

关键站点编码（已验证）：

| 站名 | 三字码 |
|---|---|
| 郑州 | ZZF |
| 郑州东 | ZAF |
| 徐州 | XCH |
| 徐州东 | UUH |
| 南京 | NJH |
| 常州 | CZH |
| 常州北 | ESH |
| 上海 | SHH |

> ⚠️ **重要发现**：郑州 ZZF 与郑州东 ZAF、常州 CZH 与常州北 ESH 是**不同车站**。
> 高铁停靠"郑州东/常州北"，普速停靠"郑州/常州"。
> **程序必须做双站组合查询**（ZZF+ZAF → CZH+ESH），否则会漏掉一半车次。

### 2.2 余票查询

```
GET https://kyfw.12306.cn/otn/leftTicket/query
    ?leftTicketDTO.train_date=2026-10-01
    &leftTicketDTO.from_station=ZZF
    &leftTicketDTO.to_station=CZH
    &purpose_codes=ADULT
```

**成功返回示例**（实测 郑州→常州，2026-10-01）：

- `httpstatus: 200`，`status: true`
- **车次总数：47 趟**
- 车型分布：`G=29, K=12, D=4, Z=2`
- 至少一个席别有票：**7 / 47**

实际车次样本：

| 车次 | 出发 | 到达 | 历时 | 二等 | 一等 | 商务 |
|---|---|---|---|---|---|---|
| K558 | 00:18 | 11:50 | 11:32 | - | - | - |
| D128 | 01:29 | 08:30 | 07:01 | 无 | - | - |
| G4824 | 04:04 | 07:43 | 03:39 | 无 | 无 | 1 |
| G92 | 10:46 | 14:04 | 03:18 | 无 | 无 | 无 |
| **G1988** | 15:25 | 20:23 | 04:58 | **有** | **有** | **6** |
| **G1950** | 18:15 | 21:57 | 03:42 | **有** | **有** | **6** |
| K4060 | 16:51 | 05:21 | 12:30 | - | - | 硬座有 |

> **接口路径简化**：老项目需要猜 `queryZ` / `queryG` / `queryA` 末字母，
> 实测**裸路径 `/otn/leftTicket/query` 也能正常返回**，不必再猜字母。

### 2.3 字段结构（58 列，`|` 分隔）

关键索引已定位：

| 索引 | 含义 | 示例值 |
|---|---|---|
| 0 | secret（加密串） | `XplgIh6E3c0...` |
| 1 | 按钮状态 | `预订` |
| 2 | train_no（内部编号） | `4a0000K55901` |
| 3 | **车次代号** | `K558` |
| 4/5 | 起讫站码 | `YWY` / `SHH` |
| 6/7 | 查询区段站码 | `ZZF` / `CZH` |
| 8 | 出发时间 | `00:18` |
| 9 | 到达时间 | `11:50` |
| 10 | 历时 | `11:32` |
| 13 | 发车日期 | `20260930` |
| 20 | 动卧 | `` |
| 21 | 高级软卧 | `` |
| 23 | 软卧 | `无` |
| 26 | 无座 | `无` |
| 28 | 硬卧 | `无` |
| 29 | 硬座 | `无` |
| **30** | **二等座** | `有` / `无` / `1` |
| **31** | **一等座** | `有` / `无` |
| **32** | **商务座** | `6` |
| 33 | 特等座 | `` |

> **余票值语义**：`有`=充足、`无`=售罄、数字=具体余量、空=该席别不存在。
> 这三个状态（有票 / 无票 / 不存在）**必须区分渲染**。

### 2.4 经停站查询

```
GET https://kyfw.12306.cn/otn/czxx/queryByTrainNo
    ?train_no=4a0000K55901
    &from_station_telecode=ZZF
    &to_station_telecode=CZH
    &depart_date=2026-10-01
```

**实测 K558 完整经停 22 站**：

```
延安 → 蒲城东 → 西安 → 渭南 → 孟塬 → 灵宝 → 三门峡 → 洛阳
→ 郑州 → 开封 → 商丘 → 徐州 → 蚌埠 → 滁州北 → 南京 → 镇江
→ 丹阳 → 常州 → 无锡 → 苏州 → 昆山 → 上海
```

返回字段：`station_no` / `station_name` / `arrive_time` / `start_time` /
`stopover_time` / `distance`

> ⚠️ **已知问题**：高铁车次（G1988、G92）经停站返回 0 条。
> 原因是 `train_no` 内部编号猜错了，需从余票接口的索引 2 动态取值。
> 普速车次（从余票接口取 train_no）验证正常。

**这正好印证了你的需求 2** —— 拿到经停站列表后，
`徐州`、`南京` 这些中间站就是天然的中转节点候选。

---

## 三、关键风险：限流实测

### 3.1 触发过程

在探测 `queryA` ~ `queryZ` 共 26 个候选字母（每次间隔 1.2 秒）后，
**余票接口被拦截**，后续所有请求均失败。

### 3.2 拦截表现（很阴险，务必注意）

```
HTTP 200                                    ← 状态码正常！
Content-Type: text/html                     ← 但类型变了
Body: 1904 字节的 HTML 错误页
      "网络可能存在问题，请您重试一下！"
```

**这是最危险的失败模式**：HTTP 200 会骗过简单的状态码检查，
JSON 解析才会暴露问题。**程序必须校验 `Content-Type` 或尝试 JSON 解析。**

### 3.3 限流范围（实测确认）

| 接口 | 限流后状态 |
|---|---|
| `leftTicket/query` 余票 | ❌ **被拦截** |
| `czxx/queryByTrainNo` 经停站 | ✅ 正常（返回 JSON 4056 字节） |
| `station_name.js` 站点字典 | ✅ 正常 |
| `12306.cn` 首页 | ✅ 正常 |

> **结论**：是**单接口级限流**，不是 IP 级封禁。
> 好消息 —— 经停站数据可以继续拉，不受余票接口限流影响。

### 3.4 冷却周期 —— ⚠️ 比预期严重得多

**实测时间线：**

| 时间点 | 状态 |
|---|---|
| 触发限流 当时 | ❌ 拦截 |
| +210 秒 | ❌ 仍拦截 |
| **+7 小时** | ❌ **仍然拦截** |
| +7 小时（经停站接口对照） | ✅ 正常，返回 22 站 |

> **这是本次验证最重要的发现**：
> 余票接口的封禁**持续 7 小时以上仍未解除**，
> 远超一般「限流」的 5-30 分钟量级。
> 这说明它更接近**较长时效的 IP+接口级风控标记**，而非简单的频率限制。

**对项目可行性的直接影响：**

原计划中「遍历所有经停站、组合查询所有换乘方案」需要
**60+ 次请求**。按 5 秒节流也只需 5 分钟——
但若累计请求量触发风控，换来的是**半天的不可用**。
**代价与收益完全不成比例。**

### 3.5 踩坑记录：重试会让情况更糟

我在脚本里实现了「失败后指数退避重试 4 次」，
结果对一个已被封禁的接口又追加了 **8 次无效请求**——
这只会延长封禁。

**正确做法：**

```
限流识别到之后 → 立即停止对该接口的一切请求
              → 冷却期 ≥ 30 分钟后再试探（只探 1 次）
              → 绝不连续重试
```

「重试」适用于网络抖动，**不适用于风控封禁**。这两者必须区分对待。

---

## 四、对方案的影响与工程对策

### 4.1 必须实现的四项机制

**① 请求节流（最重要）**

```
单接口请求间隔 ≥ 3 秒（远高于我测试的 1.2 秒）
禁止并发、禁止循环暴力试探
```

我触发限流的直接原因就是 1.2 秒间隔连发 26 次。
正常使用应控制在 **3-5 秒/次**。

**② 响应校验（防静默失败）**

```python
# 不能只看 HTTP 200
if not body.strip().startswith("{"):
    raise RateLimited("被限流，进入退避")
```

**③ 指数退避 + 本地缓存**

- 被限流后按 30s → 60s → 120s → 300s 退避
- 余票结果缓存 3-5 分钟（12306 本身也有更新间隔）
- 站点字典永久缓存，经停站缓存 1 天

**④ 控制查询规模（针对你的需求 3）**

需求 3 的"所有换乘方案"是**组合爆炸**的来源：

```
郑州→常州全程 47 趟车
× 每趟 10-22 个经停站
→ 候选中转点 20-30 个
→ 组合查询数 = 30 个中转点 × 2 段 ≈ 60 次请求
→ 按 3 秒/次 ≈ 3 分钟（还不含重试）
```

**优化策略**：

1. **优先用大站做中转**：只选省会/枢纽级车站（徐州、南京、郑州东、济南西），
   可把候选点从 30 个压缩到 5-8 个
2. **双站组合**：每个城市要查 `{普速站, 高铁站}` 两个码，组合数 ×4，务必去重
3. **两段式筛选**：先只查第一程，有票才查第二程，避免无效请求
4. **分优先级**：默认只扫 Top 5 中转站，用户可手动扩展

### 4.2 需求可行性复核

| 你的需求 | 可行性 | 关键约束 |
|---|---|---|
| 1. 列全程车次 | ✅ 已实测（47 趟） | 注意双站组合（ZZF+ZAF） |
| 2. 记录经停站 | ✅ 已实测（K558 的 22 站） | 高铁需动态取 train_no |
| 3. 所有有票换乘方案 | ✅ 可行 | **必须裁剪中转点数量** |
| 4. 运行图可视化 | ✅ 完全自主可控 | 纯前端，无接口约束 |

**第 4 项（运行图）反而是最没有风险的** —— 它只用已获取的数据做渲染，
可以独立开发，不受限流影响。建议**优先做这一块**。

---

## 五、现有开源项目的可用性评估

| 项目 | 当前可用性 | 说明 |
|---|---|---|
| `0xHJK/x12306` | ⚠️ 需修改 | 2024-07 停更，未见编码层处理 |
| `pydaxing/mcp-server-12306` | ⚠️ 需验证 | 接口最全，但同样面临加密变化 |
| `AI4MSE/TrainClaw` | ⚠️ 需验证 | 结构清晰，可参考其错误处理 |
| `Joooook/12306-skill` | ⚠️ 需验证 | 有冷却机制设计，值得借鉴 |

> **关键提醒**：实测发现 `result` 字段以 `XplgIh6E3c0...` 开头并含
> `%2F`、`%0A` 等 URL 编码序列，**与老项目文档描述的格式不完全一致**。
> 说明 12306 已加入新的编码层。
>
> 好消息：**经解析后，字段索引仍是老结构**（索引 3=车次、8=出发时间、
> 30=二等座 …），说明编码层只是外层包装，剥掉后逻辑不变。
> 但老项目若未适配这层包装，可能直接报错。

---

## 六、进展与调整后的路线

### ✅ 已完成：运行图可视化原型

用本次验证抓到的**真实数据**（47 趟车次 + K558 的 22 站经停）搭建了可交互原型：

- 文件：`运行图原型.html`（单文件、零依赖、双击即开）
- 数据：`data/route_zz_cz.json`
- 技术：原生 SVG + JS，无任何 CDN 依赖
- 零接口请求，完全不受限流影响

### ⚠️ 路线调整：不再追求「遍历式组合查询」

原计划第二步「写节流取数模块 + 第三步「遍历所有换乘组合」，
在**7 小时封禁**这个实测结论下需要重新评估：

| 原计划 | 新评估 |
|---|---|
| 遍历 30 个中转点 × 2 段 ≈ 60 次请求 | ❌ 风险过高，一次触发封禁即半天不可用 |
| 5 秒节流即可安全 | ❌ 节流只降低概率，不改变单次封禁的代价 |

**调整后的策略：**

1. **按需查询，而非预取全量**
   用户指定 1-2 个中转站（如「徐州」「南京」），只查这几个点，
   把请求量从 60 次压到 4-6 次。

2. **缓存优先，人工触发刷新**
   查询结果落盘缓存，默认读缓存；用户明确点击「刷新」才发请求。

3. **中转点推荐改为「静态推导」**
   不靠遍历查询，而是用**线路拓扑**推导候选中转站——
   郑州→常州沿陇海线转京沪线，徐州、南京是天然交汇点。
   这部分是**离线可算**的，零请求。

4. **单次会话请求上限硬约束**
   例如每次使用最多 10 次请求，超出即提示「今日查询额度已用」。

### 后续可选方向

- **A. 完善原型**：把换乘方案也纳入运行图（用静态拓扑推导的方案，标注换乘等待时间）
- **B. 隐私优先的取数**：加代理池 / 多账号轮换，降低单 IP 被封影响
- **C. 换数据源**：改用官方开放平台或第三方合规数据服务（需付费）

---

## 七、合规提醒

- 12306 接口为**非官方公开接口**，本项目仅限**个人学习与自用**
- 严禁高频请求，实测已确认会触发限流
- 严禁用于商业用途或抢票牟利
- 请勿存储、传播 12306 的原始数据
